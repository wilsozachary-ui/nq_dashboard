# ── .env.production — LOCAL_PROD environment ──────────────────────────────────
# Copy to .env.production for local production builds (npm run build).
# CRA automatically loads this file when NODE_ENV=production.
# For cloud deployment see .env.cloud.template.

REACT_APP_COCKPIT_ENV=local

# Point to your machine's LAN IP or a reverse proxy if accessing from another device
REACT_APP_API_ROOT=http://localhost:5000
# REACT_APP_WS_ROOT=ws://localhost:5000

REACT_APP_STRATEGY_NAME=morning_strategy
REACT_APP_PORT=5000
REACT_APP_LOG_LEVEL=info
REACT_APP_MARKET_OPEN_TIME=09:30
REACT_APP_MARKET_CLOSE_TIME=16:00
REACT_APP_TZ=America/New_York
